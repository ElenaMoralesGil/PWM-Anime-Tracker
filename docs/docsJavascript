/*
    let contentPromise = fetch(routeCharacter).
    then(response => response.text()).
    then(html => {
        contentHTML = html;
    });

    let imagePromise = fetch(routeImage).
    then(response => response.text()).
    then(html => {
        imageHTML = html;
    });
*/